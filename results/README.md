# Results artifacts

This folder contains the fixed seed-11 source examples, hosted-LLM prompt files, manually collected hosted-LLM responses, generated augmentation examples, and final downstream metrics.

The main table is:

```text
st04_one_seed_downstream_metrics.csv
```

These artifacts are included so the repository can show the completed MVP result without requiring a fresh hosted-LLM generation run.
